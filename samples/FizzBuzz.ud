use System;

module FizzBuzz
{
    function Main()
    {
        for (var i in 1..100)
        {
            if (i % 3 = 0)
            {
                IO.Print("Fizz");
            }

            if (i % 5 = 0)
            {
                IO.Print("Buzz");
            }

            if ((i % 3 != 0) and (i % 5 != 0))
            {
                IO.Print(i);
            }

            IO.Print("\n");
        }
    }
}